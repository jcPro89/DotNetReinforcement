@model Department

@{
    ViewData["Title"] = "_AddDepartmentPartialView";
}

<div class="modal fade" role="dialog" tabindex="-1" id="addDepartment" aria-labelledby="addDepartmentLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3>Department</h3>
            </div>
            <div class="modal-body">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="DepartmentName" class="control-label">Department Name</label>
                        <input asp-for="DepartmentName" class="form-control" />
                        <span asp-validation-for="DepartmentName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="DepartmentLocation" class="control-label">Department Location</label>
                        <input asp-for="DepartmentLocation" class="form-control" />
                        <span asp-validation-for="DepartmentLocation" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="NumberOfEmployees" class="control-label">Number of employees</label>
                        <input asp-for="NumberOfEmployees" class="form-control" id="NumberOfEmployeesInput" type="text"/>
                        <span asp-validation-for="NumberOfEmployees" class="text-danger"></span>
                    </div>

                    <h3>Employees</h3>
                    <table class="table table-bordered" id="employeesTable">
                        <thead>
                            <tr>
                                <th>Employee Name</th>
                                <th>Employee Job</th>
                                <th>Email</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for(int i = 0; i < Model.Employees.Count; i++)
                            {
                                <tr>
                                    <td>
                                        @Html.EditorFor(e=>e.Employees[i].EmployeeName, new { htmlAttributes = new { @class = "form-control"}})
                                    </td>
                                    <td>
                                        @Html.EditorFor(e=>e.Employees[i].EmployeeJob, new { htmlAttributes = new { @class = "form-control"}})
                                    </td>
                                    <td>
                                        @Html.EditorFor(e=>e.Employees[i].Email, new { htmlAttributes = new { @class = "form-control"}})
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <input type="hidden" id="LastIndex" value="0" />
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>                    
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    @* Generate specified amount of input boxes for adding employees *@
    $("#NumberOfEmployeesInput").on('change', function (){
        var RowNumber = document.getElementById('NumberOfEmployeesInput').value;
        $("#employeesTable tbody").find("tr:not(first)").remove();

        for(var i = 0; i < RowNumber - 1; i++){
                var table = document.getElementById('employeesTable');
                var rows = table.getElementsByTagName('tr');
                var rowOuterHtml = rows[rows.length - 1].outerHTML;
                var lastRowIndex = document.getElementById('LastIndex').value;

                var nextRowIndex = eval(lastRowIndex) + 1;

                document.getElementById('LastIndex').value = nextRowIndex;
                rowOuterHtml = rowou.replaceAll('_' + lastRowIndex + '_', '_' + nextRowIndex + '_');
                rowOuterHtml = rowou.replaceAll('_' + lastRowIndex + '_', '_' + nextRowIndex + '_');
                rowOuterHtml = rowou.replaceAll('_' + lastRowIndex, '_' + nextRowIndex);

                var newRow = table.insertRow();
                newRow.innerHTML = rowOuterHtml;
        }
    })
</script>